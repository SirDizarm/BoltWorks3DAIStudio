# BWS 50.0.33

Axis buttons recenter and fit visible model bounds, reset zoom, and choose distance for the viewport aspect ratio. Side-view target height stays above the grid; an empty scene frames a raised default centre. Negative Y retains the bottom-view direction.

Source and browser bundle synchronized. No interaction tests run for this change.
