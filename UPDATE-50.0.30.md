# BWS 50.0.30

Animator transport wraps without shrinking buttons; frame readout and key actions have separate rows. Rotation fields name their axes and degree units. Main viewport uses the Geometry Nodes axis guide with six direction buttons; constrained work views and locked cameras retain their constraints.

Source and root browser bundle rebuilt together. No interaction tests run.
