# BWS 50.0.31

Main viewport axis clicks clear pending OrbitControls damping before setting the requested direction, then restore normal damping. Axis guide moved to the top-right corner with an 8px inset.

Canonical viewport source and browser bundle rebuilt together. No interaction tests run.
