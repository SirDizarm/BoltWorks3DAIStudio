# BWS 50.0.32

Axis-view buttons now disable cursor-targeted zoom so wheel zoom retains the view target. Pointer navigation on the canvas restores cursor zoom in unconstrained views.

Viewport source and browser bundle synchronized. No interaction tests run.
