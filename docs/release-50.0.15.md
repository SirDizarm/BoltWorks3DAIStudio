# BWS 50.0.15

- Browse plugins inside BWS: descriptions, install/update buttons and the existing permission review.
- No background package downloads or automatic enabling.
- Private-repository failures explain the signed-in download and Install from file fallback.
- Plugin READMEs prominently identify the single ready-to-install package and separate developer build instructions.
