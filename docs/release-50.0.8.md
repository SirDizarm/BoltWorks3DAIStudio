# BWS 50.0.8

- Renamed installation action to Install plugin (Update plugin for replacements).
- Explained that plugins are off by default and can be enabled in Plugins. Installation behavior is unchanged.
