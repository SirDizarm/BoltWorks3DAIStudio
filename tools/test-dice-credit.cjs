const {chromium}=require(process.env.BWS_PLAYWRIGHT || 'playwright');const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});try{const page=await browser.newPage({viewport:{width:1200,height:900}});await page.route(/^https?:/,route=>route.abort());await page.goto('file:///'+process.cwd().replaceAll('\\','/')+'/demos/BWS-dice-randomizer.html');const credit=page.locator('#creator-credit');assert.ok(await credit.isVisible());assert.equal(await credit.innerText(),'Created by Daniel Rydin · Made in BoltWorks 3D AI Studio');await page.screenshot({path:'demos/dice-export-credit-preview.png'});console.log('PASS: visible creator and BoltWorks credit in offline HTML.');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
