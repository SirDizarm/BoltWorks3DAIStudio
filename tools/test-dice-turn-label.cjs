const {chromium}=require(process.env.BWS_PLAYWRIGHT || 'playwright');const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});try{const page=await browser.newPage();await page.goto('file:///'+process.cwd().replaceAll('\\','/')+'/demos/BWS-dice-randomizer.html');await page.locator('.dice-players summary').click();await page.getByLabel('Player 1 name',{exact:true}).fill('D');await page.getByLabel('Player 2 name',{exact:true}).fill('R');const turn=page.locator('.dice-score-turn');assert.equal(await turn.innerText(),'D’s turn');await page.getByRole('button',{name:'Next player',exact:true}).click();assert.equal(await turn.innerText(),'R’s turn');await page.locator('.dice-players summary').click();assert.ok(await turn.isVisible());await page.locator('#roll').click();assert.equal(await turn.innerText(),'R’s turn');console.log('PASS: live renamed turn label, next player, collapsed panel, roll preserves turn.');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
